<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.aeroscope.aeroscope">

    <!-- the following probably won't work, as Android 6 apparently wants explicit grant of permissions, and Location Services enabled -->
    <uses-permission android:name="android.permission.BLUETOOTH" />              <!-- do anything with Bluetooth: NORMAL -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />        <!-- discover and pair devices: NORMAL -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- required for scanning in Android >5: DANGEROUS -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="io.aeroscope.aeroscope.AeroscopeApp">

        <service
            android:name="io.aeroscope.aeroscope.AeroscopeBluetoothService">
        </service>

        <activity
            android:name="io.aeroscope.aeroscope.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AeroscopeDisplay">
        </activity>

    </application>

</manifest>